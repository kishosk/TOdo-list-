apiVersion: v1
kind: Service

metadata:
  name: frontend-service

spec:
  selector:
    app: {{ include "todo.frontend.name" . }}

  ports:
  - port: {{ .Values.frontend.service.port }}
    targetPort: {{ .Values.frontend.service.port }}
    nodePort: {{ .Values.frontend.service.nodePort }}

  type: {{ .Values.frontend.service.type }}