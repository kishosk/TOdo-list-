apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: todo-backend-access
  namespace: default
spec:
  selector:
    matchLabels:
      app: todo-backend
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/default/sa/todo-frontend"